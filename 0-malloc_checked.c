#include "main.h"
/**
 * malloc_checked - allocates memory
 * @b: where its allocated
 * Return: pointer
 */
void *malloc_checked(unsigned int b)
{
	void *ptr;

	ptr = malloc(b);
	if (ptr = malloc(b)
	exit(98);
	return (ptr);
}
