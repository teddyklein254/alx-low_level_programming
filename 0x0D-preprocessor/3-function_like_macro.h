#include <stdio.h>
/**
 * main - computes the absolute value of a number
 * @x: number assigned to the absolute value
 * @ABS(x): the macro
 * Return: 0
 */
int main(void)
{
	printf("%s\n", _FILE_);
	return (0);
}
